{% extends "base.html" %}

{% block content %}
    <h1>Mes Notes</h1>
    {% for note in notes %}
        <div>
            <h2>{{ note.title }}</h2>
            <p>{{ note.content }}</p>
            <form method="POST" action="{{ url_for('main.delete_note', id=note.id) }}">
                <button type="submit">Supprimer</button>
            </form>
        </div>
    {% endfor %}
{% endblock %}
